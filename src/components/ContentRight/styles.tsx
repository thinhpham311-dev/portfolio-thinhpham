import tw, { styled, css, theme } from 'twin.macro'

const ContentRightSectionWrapper = styled.main(() => [
    tw`xl:w-9/12 lg:w-8/12 md:w-full sm:w-full w-full h-full p-5`
])

export {
    ContentRightSectionWrapper
}